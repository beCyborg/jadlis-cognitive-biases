# Changelog — cognitive-biases

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии — [SemVer](https://semver.org/lang/ru/).
До 1.0.0 совет жил в плагине `advisors` 1.0.0–1.0.3 (репо `advisors`, теперь приватный источник `jadlis-advisors-source`).

## [Unreleased]

## [1.0.3] — 2026-09-22 — Переход на Opus 5.5 / Switch to Opus 5.5

### Для человека

- Советник теперь работает на новой модели Opus 5.5 с окном контекста в 1 млн токенов.
- Требует Claude Code ≥ 2.1.280: более старые версии могут не знать эту модель — сначала обновите Claude Code, потом плагин.
- Принципы, теги-ссылки и формат ответа не менялись.

### For agents

- Changed: `skills/cognitive-biases/SKILL.md` — frontmatter `model: opus` → `model: claude-opus-5-5`.
- Migration: requires Claude Code ≥ 2.1.280 — older versions may reject the unknown model ID `claude-opus-5-5` when loading the skill; update Claude Code (`claude update`). The 1M context window is native to the model — no `[1m]` suffix.

## [1.0.2] — 2026-09-08

### Для человека

- Обновление из источника jadlis-advisors-source.

### For agents

- Regenerated by `tools/split-councils.py`; see the source repo commit in `SHARED_FROM.txt`.

## [1.0.0] — 2026-09-07

### Для человека

- Первый релиз под именем `cognitive-biases`: выделен из `advisors` 1.0.3 — репо на совет, команда `/cognitive-biases`, ключ настройки `MEMORY_DIR`.

### For agents

- Generated by `tools/split-councils.py` of `jadlis-advisors-source`; worker agent `cognitive-biases:advisor-opus`; `SHARED_FROM.txt` carries the source tag + content hash checked by CI.
